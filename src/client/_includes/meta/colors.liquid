<!-- Colors -->
  <meta name="theme-color" content="{{ site.colors.primary }}">
  <!-- iOS status bar -->
  <meta name="apple-mobile-web-app-status-bar-style" content="default">
  <!-- Windows 8 IE 10-->
  <meta name="msapplication-TileColor" content="{{ site.colors.primary }}">